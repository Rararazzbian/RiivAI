{
  "name": "user_traits",
  "description": "Manage user traits, this is only for specific users, not servers.",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "name": "action",
        "type": "string",
        "description": "The action to perform (get_traits, add_trait, remove_trait, reset_traits))"
      },
      "user_id": {
        "name": "user_id",
        "type": "string",
        "description": "The user ID"
      },
      "trait": {
        "name": "trait",
        "type": "string",
        "description": "Short sentence trait"
      }
    }
  }
}